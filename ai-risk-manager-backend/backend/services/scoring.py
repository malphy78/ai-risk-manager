def calculate_risk_score(csv_data: str):
    # Placeholder: parse CSV and calculate a fake score
    lines = csv_data.strip().split("\n")
    if len(lines) < 2:
        return "Insufficient data"
    return "Medium Risk"
